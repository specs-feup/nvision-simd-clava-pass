{
  "name": "simd-clava-pass",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/js/index.js",
  "files": [
    "src",
    "test",
    ".gitignore",
    "jest.config.js",
    "package-lock.json",
    "package.json",
    "tsconfig.jest.json",
    "tsconfig.json"
  ],
  "scripts": {
    "test": "cross-env NODE_OPTIONS=\"$NODE_OPTIONS --experimental-vm-modules\" jest --detectOpenHandles --forceExit src",
    "clear": "rm -rf dist",
    "clearall": "rm -rf dist node_modules package-lock.json",
    "build": "tsc",
    "forloop": "npx clava classic dist/js/index.js -j -p src/input/forloop.c -o dist/",
    "buildrunforloop": "npm run clear && npm run build && npm run forloop"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@specs-feup/clava": "3.0.11",
    "typescript": "^5.8.2"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.14",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4"
  }
}